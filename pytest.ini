[pytest]
# pythonpath=. puts the repo root on sys.path so `import backend.app...` works
# no matter how pytest is invoked (bare `pytest` in CI doesn't add the CWD).
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
